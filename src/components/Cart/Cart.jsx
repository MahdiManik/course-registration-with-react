
import PropTypes from 'prop-types'

const Cart = cart => {
  return (
    <div>
      <h3 className='text-3xl font-semibold'>Cart: {courses.length}</h3>
    </div>
  )
}

Cart.propTypes = {
  cart.PropTypes.object
}

export default Cart